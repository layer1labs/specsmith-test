#!/usr/bin/env bash
set -euo pipefail
PROJECT_ROOT="$(cd "$(dirname "$0")/.." && pwd)"

echo "specsmith-test setup (Linux/macOS)"
# Non-Python project. Add project-specific setup steps here.
echo "NOTE: Edit scripts/setup.sh to add setup steps for embedded."
echo "Setup complete."
